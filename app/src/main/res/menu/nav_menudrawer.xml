<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="Events"
        android:id="@+id/general_events">
        <menu>
            <group android:id="@+id/allevents">
                <item
                    android:orderInCategory="0"
                    android:id="@+id/all"
                    android:icon="@drawable/all_white"
                    android:title="@string/all" />
            </group>
            <group android:id="@+id/events">
            </group>

        </menu>
    </item>
    <item android:title="Connections"
        android:id="@+id/general_connections">
        <menu>
            <group android:id="@+id/allconnections">
                <item
                    android:orderInCategory="0"
                    android:id="@+id/all_cnct"
                    android:icon="@drawable/all_white"
                    android:title="All Connections" />
            </group>
            <group android:id="@+id/connections">
            </group>

        </menu>
    </item>
    <group android:id="@+id/settings">
        <item
            android:orderInCategory="2"
            android:id="@+id/user_settings"
            android:icon="@drawable/settings_white"
            android:title="@string/settings"/>
    </group>

</menu>